# Your API key from https://atlasfetch.xyz/dashboard.
# Leave unset to fall back to the shared public demo key, whose quota and rate
# limit are pooled across every anonymous user — evaluation only.
ATLASFETCH_API_KEY=

# Override only when pointing at a local or staging API.
# ATLASFETCH_API_URL=https://api.atlasfetch.xyz
